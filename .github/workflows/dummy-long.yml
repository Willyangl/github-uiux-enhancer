name: Dummy Long Run

on:
  workflow_dispatch:

jobs:
  wait:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1 - Start
        run: echo "Starting dummy workflow..."

      - name: Step 2 - Wait 1 minutes
        run: sleep 60

      - name: Step 3 - Done
        run: echo "Dummy workflow completed."
